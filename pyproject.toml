[project]
name = "holy-sunset-5828"
version = "0.1.0"
description = "FastAPI chatbot with Groq and Pollinations"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "groq>=1.7.0",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
